# ============================================
# ddev.online domain
# ============================================

# www.ddev.online -> ugc-ui
server {
    listen 80;
    server_name www.ddev.online;

    location / {
        proxy_pass http://ugc.ui:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# api.ddev.online -> ugc-api
server {
    listen 80;
    server_name api.ddev.online;

    client_max_body_size 100M;

    location / {
        proxy_pass http://ugc.api:80;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# admin-api.ddev.online -> ugc-admin-api
server {
    listen 80;
    server_name admin-api.ddev.online;

    client_max_body_size 100M;

    location / {
        proxy_pass http://ugc.admin-api:80;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# admin.ddev.online -> ugc-admin
server {
    listen 80;
    server_name admin.ddev.online;

    location / {
        proxy_pass http://ugc.admin:80;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
